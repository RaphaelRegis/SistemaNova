spring:
  application:
    name: ConfigServer

server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/RaphaelRegis/SistemaNova/tree/main/configurations